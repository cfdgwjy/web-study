                                                      Proxy
  Proxy用于修改某些操作的默认行为，等于在语言层面进行修改，相当于"元编程"。即对编程语言进行编程。
  
  
  proxy可以理解为，在目标对象之前设一层拦截，外界对这个对象的访问，都必须通过这层拦截，因此提供了一种机制。它可以对外界的访问进行过滤和改写。proxy原意为代理。
  例子：
  
  ES6原生提供Proxy()构造函数，用于生成proxy实例。
  var proxy=new Proxy(target,handler)
  Prxoy对象用法都是上面这个形式，不同的是handler写法。其中，new Proxy()表示生成一个
