栈，后进先出，最新的元素存在栈顶。
function stack()
{
var items = []; 
}
栈方法： push(添加一个或几个元素到栈顶),pop（移除栈顶的元素，同时返回被移除元素）,peek（返回栈顶元素） isEmpty(栈有元素？true,false)
clear(清除栈中所有元素)  size:返回栈里的元素个数

//栈的全部代码
function Stack()
{
var items=[];
this.push=function(element)
{
items.push(element);
}
this.pop=function(element)
{
  return items.pop();
}
this.clear=function()
{
 items=[];
}
}

//使用
var stack=new Stack();
stack.push(5);
stack.pop();




